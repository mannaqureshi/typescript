// order can be broken down
const pepsi = ["brown", true, 40];
pepsi[0] = 40;

type Drink = [string, boolean, number];

const sprite: Drink = ["clear", true, 40];
const tea: Drink = ["brown", true, 40];

// tuple doest provide meaning, just structure
